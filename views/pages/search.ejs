<h1>Search Results</h1>
<div class="">

      <% businesses.forEach((b) => { %>
        <div class="row">
            <a href="/location/<%= b.id %>"><img src="<%= b.image_url %>" alt=""><%= b.name %> (<%= b.location.city %>) </a>
          </div>
          <% }) %>

</div>
