<div class="row">
    <div class="col-sm-6 col-sm-offset-3">
      <h2><% businesses.forEach((b) => { %>
          <%= b.name %>(<%= b.city %>)</h2>

      <% }) %>







      <!-- <div class="well">
        <ul class="list-group">
          <% businesses.reviews.forEach((l) => { %>
            <li class="list-group-item">
              <h3>
                <a href="/locations/<%=l._id%>">
                <%= l. title %></h3>
            </li>
          <% }) %>
        </ul>
      </div> -->
    </div>
</div>
